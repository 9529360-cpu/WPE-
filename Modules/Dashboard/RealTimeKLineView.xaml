<UserControl x:Class="币安量化机器人.Modules.Dashboard.RealTimeKLineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="600">
    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="6" Padding="8">
        <Grid>
            <StackPanel>
                <TextBlock Text="实时K线（占位）" HorizontalAlignment="Center" VerticalAlignment="Top" FontWeight="Bold" Margin="0,4"/>
                <TextBlock x:Name="ProgressSummaryText" Text="项目进度：示例更新 - 1/5 完成" HorizontalAlignment="Center" Margin="0,4" FontStyle="Italic" Foreground="Gray"/>
            </StackPanel>
            <Grid Margin="0,56,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="120" />
                </Grid.RowDefinitions>

                <Canvas Grid.Row="0" x:Name="PriceCanvas" Background="Transparent" />
                <Canvas Grid.Row="1" x:Name="VolumeCanvas" Background="Transparent" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
