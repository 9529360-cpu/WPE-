<UserControl x:Class="币安量化机器人.Modules.Risk.RiskManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:币安量化机器人.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <vm:RiskManagerViewModel x:Key="RiskVM" />
    </UserControl.Resources>
    <Grid Margin="12">
        <StackPanel>
            <TextBlock Text="风控管理" FontWeight="Bold" FontSize="16"/>

            <Grid Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="单笔最大数量：" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Text="{Binding MaxOrderQuantity, Source={StaticResource RiskVM}, Mode=TwoWay}"/>

                <TextBlock Grid.Column="0" Text="最大持仓量：" VerticalAlignment="Center" Margin="0,36,0,0"/>
                <TextBox Grid.Column="1" Text="{Binding MaxPositionSize, Source={StaticResource RiskVM}, Mode=TwoWay}" Margin="0,36,0,0"/>

                <TextBlock Grid.Column="0" Text="日损阈值：" VerticalAlignment="Center" Margin="0,72,0,0"/>
                <TextBox Grid.Column="1" Text="{Binding DailyLossLimit, Source={StaticResource RiskVM}, Mode=TwoWay}" Margin="0,72,0,0"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="保存" Width="90" Command="{Binding SaveCommand, Source={StaticResource RiskVM}}"/>
                <Button Content="刷新" Width="90" Margin="8,0,0,0" Command="{Binding RefreshCommand, Source={StaticResource RiskVM}}"/>
            </StackPanel>

            <Border Margin="0,16,0,0" Padding="8" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="当前违规记录（示例）" FontWeight="Bold"/>
                    <ListBox ItemsSource="{Binding Breaches, Source={StaticResource RiskVM}}" Height="160"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
