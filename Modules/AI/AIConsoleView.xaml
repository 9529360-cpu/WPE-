<UserControl x:Class="币安量化机器人.Modules.AI.AIConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="AI 控制台" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <!-- Signals list -->
            <GroupBox Header="实时信号" Margin="4" Grid.Column="0">
                <ListView ItemsSource="{Binding Signals}" SelectedItem="{Binding SelectedSignal}" >
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="时间" DisplayMemberBinding="{Binding Timestamp}" Width="140" />
                            <GridViewColumn Header="交易对" DisplayMemberBinding="{Binding Symbol}" Width="120" />
                            <GridViewColumn Header="操作" DisplayMemberBinding="{Binding Action}" Width="80" />
                            <GridViewColumn Header="置信度" DisplayMemberBinding="{Binding Confidence}" Width="80" />
                            <GridViewColumn Header="理由" DisplayMemberBinding="{Binding Reason}" Width="220" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </GroupBox>

            <!-- Right side: events, gates, strategy changes -->
            <StackPanel Grid.Column="1" Margin="4" Orientation="Vertical">
                <GroupBox Header="回测/模拟/实盘事件" Margin="0,0,0,8">
                    <ListBox ItemsSource="{Binding TradingEvents}" Height="150">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </GroupBox>

                <GroupBox Header="闸门拒单原因" Margin="0,0,0,8">
                    <ListBox ItemsSource="{Binding GateRejections}" Height="120">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Reason}" TextWrapping="Wrap" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </GroupBox>

                <GroupBox Header="策略替换记录" Margin="0,0,0,8">
                    <ListBox ItemsSource="{Binding StrategyChanges}" Height="100">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </GroupBox>

                <GroupBox Header="因子参数微调记录" Margin="0,0,0,8">
                    <ListBox ItemsSource="{Binding ParameterAdjustments}" Height="100">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </GroupBox>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
