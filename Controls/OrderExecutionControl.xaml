<UserControl x:Class="币安量化机器人.Controls.OrderExecutionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid Margin="8">
        <StackPanel>
            <TextBlock Text="委托执行" FontWeight="Bold" FontSize="14"/>
            <DataGrid x:Name="OrdersGrid" AutoGenerateColumns="False" Height="120" Margin="0,8,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="订单ID" Binding="{Binding Id}" Width="*"/>
                    <DataGridTextColumn Header="交易对" Binding="{Binding Symbol}" Width="80"/>
                    <DataGridTextColumn Header="类型" Binding="{Binding Side}" Width="60"/>
                    <DataGridTextColumn Header="数量" Binding="{Binding Quantity}" Width="80"/>
                    <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                    <DataGridTemplateColumn Header="操作" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="撤单" Margin="0,0,6,0"/>
                                    <Button Content="修改"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
